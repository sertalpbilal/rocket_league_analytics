version: '3'
services:
  rocket_league_analysis:
    container_name: main
    build: .
    environment:
      - PYTHONUNBUFFERED=1
      - LANG=C.UTF-8
    env_file: .env
    volumes:
      - ./data:/data
      - ./src:/src
      # - ..env:/.env
